const RIOREUS = {
  id  : 0,
  type: '飛竜種',
  name: 'リオレウス',
}
const RIOREIA = {
  id  : 1,
  type: '飛竜種',
  name: 'リオレイア',
}

let monster = RIOREUS
let items = []

if (monster.name === 'リオレウス') {
  items.push('閃光弾')
} else if (monstar.name === 'リオレイア') {
  items.push('解毒薬')
}

// console.log(items)
